<table class="grid" cellpadding="0" cellspacing="0">
		<thead>
			<tr>
				<th>Name</th>
				<th>Role</th>
				
			</tr>
        </thead>
<tbody>

	<% @users.each do |user| %>
		<tr>
		<td><%= user.email %></td>
		<td><%= user.role.name %></td>	
		</tr>

	<% end %>
</tbody>
</table>

<%= link_to 'New User', action: 'new' %>