<%= form_for @user do |f| %>
	<fieldset>
		<legend>Edit User</legend>
      <%= render partial: 'form', locals: {f:  f} %>
		<%= submit_tag 'Edit' %> <%= link_to 'Cancel', user_path %>
	</fieldset>
<% end %>