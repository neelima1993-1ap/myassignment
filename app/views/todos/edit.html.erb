<%= form_for @todo do |f| %>
	<fieldset>
		<legend>Edit Task</legend>
      <%= render partial: 'form', locals: {f:  f} %>
		<%= submit_tag 'Edit' %> <%= link_to 'Cancel', todos_path %>
	</fieldset>
<% end %>